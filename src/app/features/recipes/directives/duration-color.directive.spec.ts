import { DurationColorDirective } from './duration-color.directive';

describe('DurationColorDirective', () => {
  it('should create an instance', () => {
    const directive = new DurationColorDirective();
    expect(directive).toBeTruthy();
  });
});
